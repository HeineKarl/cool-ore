import { createStore } from "vuex";
import { textToSpeech } from "./modules/textToSpeech";

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    textToSpeech,
  },
});
